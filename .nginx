location ~* \.html$ {
    rewrite ^(.+)\.html$ $scheme://$host$1 permanent;
}

if ($http_user_agent ~* NetShelter) {
    return 403;
}

location ~ ^/(mmp-preorders|mastering-modern-payments)$ {
    if ($scheme  = "http") {
        rewrite ^/(.*)$  https://$host/$1 permanent;
    }
}